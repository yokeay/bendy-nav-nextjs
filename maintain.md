# Maintain

## v2.4.37

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续按旧前台设置组件收口字段顺序，并补齐已经有数据但前端没接上的设置行为。
### 本次修改

- 把 `pageGroupPosition` 真正接到首页侧栏布局，设置里新增 `侧栏位置`，右侧侧栏时工具栏与主内容会同步避让。
- 把 `sideBackground` 接回数据层、设置入口和侧栏样式，`Dock / 侧边背景色` 现在会真实作用到首页侧栏。
- 按旧前台控制中心的顺序重排设置页：`常规设置` 调整搜索项顺序，`主题标签` 收拢 `Dock / 侧栏` 与 `图标和字体`，`时间日期` 把颜色项移到底部。
- 把 `opacity` 调回旧版语义：壁纸区改成 `遮罩透明度` 的 `0-1` 滑杆，并把它真正作用到首页背景遮罩，不再是无效的 0-100 假设置。
### 迁移结果

- 设置中心进一步从“字段大致齐了”推进到“关键分区顺序、字段语义和页面行为开始一致”。
- `侧栏位置` 和 `遮罩透明度` 现在不再只是数据层字段，而是前台可见、可验证的实际能力。
### 风险与后续

- `LinkTitle` 等旧版字段仍未完全补齐或对齐，设置页和旧版之间还存在剩余缺口。
- 当前时间区仍用单行开关列表承载旧版的宫格选项语义，后续如果继续追 1:1，需要再压布局形态。

## v2.4.36

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：清理首页动作弹层的旧实现，把添加/编辑入口彻底收口到当前组件。
### 本次修改

- 删除 `home-actions.tsx` 中遗留的旧添加/编辑弹层实现，避免继续维护两套请求、状态和表单逻辑。
- 保留并继续转发 `home-link-editor-dialog.tsx` 的 `AddLinkDialog`，让首页添加/编辑入口只剩单一实现。
- 给壁纸库和页面图标选择器的旧接口读取补上数组兜底，避免异常响应直接打断弹层渲染。
### 迁移结果

- 首页动作弹层现在从“新实现已接入，但旧实现还躺在同文件里”推进到“主路径只保留一套实现”。
- 后续继续压添加/编辑弹层时，不再需要同步修改一份未使用的遗留代码。
### 风险与后续

- 设置页字段顺序与 `latest/mtab` 仍未完全逐项对齐，下一轮继续对照处理。
- `home-actions.tsx` 现在更干净了，但后续如果还要继续拆动作面板，建议把背景和页面管理也进一步模块化。

## v2.4.35

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续压设置中心和添加弹层的控件密度，不扩新功能面。
### 本次修改

- 继续收紧添加/编辑弹层的窗口内边距、表单行间距、页签容器和目标页面下拉样式，让它更接近 `mtab custom` 的密度。
- 设置中心继续补控件质感：用户卡 hover、菜单 hover、操作行、白底设置行、深色主题下的行级背景、范围条与开关底板都进一步统一。
- 通过 CSS 让操作箭头完全由样式生成，避免遗留字符干扰显示。
### 迁移结果

- 当前设置中心和添加弹层已经明显从“能用”推进到“结构和控件密度开始像目标项目”。
- 剩余差距继续收敛到字段顺序、局部留白和旧实现清理，不再是主视图骨架问题。
### 风险与后续

- `home-actions.tsx` 里遗留的旧添加弹层实现还在，后续应清理，避免维护两套实现。
- 设置页字段顺序和个别分区仍未完全与 `latest/mtab` 逐项一致，下一轮继续对照处理。

## v2.4.34

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续把设置中心压回 `latest/mtab` 的信息密度和控件质感，不扩新方向。
### 本次修改

- 设置中心每次打开都会回到 `个人中心`，不再停留在上一次的子页状态。
- `设置行 / 操作行 / 范围条 / 色板 / 开关` 的视觉密度收紧成更接近 `mtab` 的白底 45px 行项目。
- 左侧用户卡和菜单 hover、右侧操作箭头、深色主题下的行级背景也一起补齐，整体更接近 macOS 风格的轻阴影与弱层级。
- 添加/编辑弹层的图标选择尺寸和范围条细节也顺手压了一层，减少自定义感。
### 迁移结果

- 当前设置中心已经从“结构相似”往“控件质感和信息密度相似”推进了一步。
- 这轮之后，最主要的剩余差距更集中在设置字段顺序、局部留白，以及添加/编辑弹层的像素级布局，而不是主交互链路。
### 风险与后续

- `home-actions.tsx` 里仍有遗留的旧添加弹层实现，后续在新弹层完全收口后应清掉。
- 设置中心字段顺序和少数字段分区位置仍未完全与 `latest/mtab` 1:1，对照工作还要继续。

## v2.4.33

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：把 `编辑标签 / 新增标签 / 添加卡片` 的高优先缺口继续收口，补回推荐标签、目标页面选择和推送标签链路。
### 本次修改

- 新增 [home-link-editor-dialog.tsx](/E:/codeData/JavaScriptProject/bendy-nav-nextjs/src/features/home/home-link-editor-dialog.tsx)，直接接管添加/编辑弹层，避免继续在旧实现上叠补丁。
- `添加标签 / 推荐标签 / 添加卡片` 现在收口到同一个弹层里，且都支持先选目标页面再落到指定页面。
- 添加标签接回 `LinkStore/list`、`LinkStore/getIcon` 和 `LinkStore/push`，补回推荐标签、站点图标获取和推送标签链路。
- 添加卡片与普通标签创建现在都会按目标页面计算排序，不再全局乱序落位。
### 迁移结果

- 这轮高优先项里，添加弹层已经从“能用”推进到“功能链路开始接近 `latest/mtab` + iTab 的组合目标”。
- 当前差距开始集中在弹层密度、图标选择区布局、设置页字段顺序和视觉细节，不再是推荐标签/目标页面这种主功能缺失。
### 风险与后续

- 旧的 `home-actions.tsx` 内部仍保留一份遗留添加弹层实现，后续应在对齐完成后彻底清理。
- 设置页功能虽然可用，但与 `latest/mtab` 的字段顺序、分区密度和视觉细节还需要继续压。

## v2.4.32

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：把“添加卡片”真正接回首页主路径，补齐高优先四项中的最后一条核心能力。

### 本次修改

- `添加标签` 窗口增加 `添加卡片` 视图，可直接读取 `/card/index` 的卡片列表并展示预览。
- 添加卡片后会按 `component=plugins` 的形式写入首页数据，并同步上报卡片安装量。
- 首页开始真正渲染插件卡片，不再把 `component: plugins` 卡片过滤掉。

### 迁移结果

- 高优先四项里 `编辑标签 / 新增标签 / 添加卡片 / 设置页面` 都已经有了可用主链路，不再缺整块功能。
- 当前差距开始集中到窗口尺寸、密度、按钮位置和视觉细节，而不是“功能有没有”。

### 风险与后续

- 添加卡片当前已经可用，但入口和窗口结构仍需继续往 `latest/mtab` 的交互细节压。
- 插件卡片的桌面渲染目前走 iframe 预览，后续还要继续收它和原版在细节上的差异。

## v2.4.31

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续压设置中心视觉一致性，把左侧菜单和游客页动作入口接到 `latest/mtab` 的图标资源。

### 本次修改

- 从 `latest/mtab` 的本地 CSS 中提取了设置中心所需的 `iconfont / bizhi / history` 图标字体资源，新建 [home-settings-icons.module.css](E:/codeData/JavaScriptProject/bendy-nav-nextjs/src/features/home/home-settings-icons.module.css) 供设置中心复用。
- 设置中心左侧菜单和游客页动作入口改用 `mtab` 同源图标，不再使用字符占位。
- 设置中心菜单图标、导入/导出/重置入口图标已统一接入这套本地图标资源。

### 迁移结果

- 设置中心左侧视觉已经明显更接近 `mtab`，不再是纯文本菜单。
- 当前差距继续从“结构不像”缩小到“间距、字号、细部排版是否完全一致”这一层。

### 风险与后续

- 图标资源已经对上，但设置中心内部字段排布和操作密度仍然需要继续微调。
- 编辑标签弹层和设置中心都还需要继续往 `latest/mtab` 的窗口尺寸和行距压到 1:1。

## v2.4.30

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续按 `latest/mtab` 收口设置中心结构，把游客页/个人中心与备份导入补成真实链路。

### 本次修改

- 设置中心顶部用户卡改成可点击的独立“个人中心/游客页”入口，不再只是静态信息块。
- 游客页补回 `导入书签备份`，并打通到首页 JSON 备份导入链路。
- `常规设置` 与 `数据变动记录` 的职责继续拆正：
  - 搜索历史开关留在常规设置
  - 历史快照恢复保留在数据变动记录

### 迁移结果

- 设置中心当前已经具备更接近 `mtab` 的“个人中心页 + 六项功能菜单”结构。
- 导入/导出/重置/快照恢复这几条数据管理链路都已经真实可用，不再只是按钮占位。

### 风险与后续

- 左侧菜单图标仍然不是 `mtab` 原版图标资源，后续还要继续压。
- 设置中心内部各分区的字段顺序和视觉密度仍需继续对齐。

## v2.4.29

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续把设置中心向 `mtab` 本地基线收口，补齐游客页/个人中心和备份导入链路。

### 本次修改

- 设置中心顶部用户卡改成可点击的独立“个人中心/游客页”入口，不再只是静态展示块。
- 个人中心/游客页补回了 `导入书签备份`，并打通到首页真实的 JSON 导入链路。
- 常规设置和数据变动记录分区继续调整为更接近 `mtab` 的功能分布：搜索历史开关留在常规设置，数据快照恢复保留在数据变动记录。

### 迁移结果

- 设置中心当前已经具备 `立即登录 / 导入备份 / 导出备份 / 重置标签 / 搜索历史开关 / 历史快照恢复` 这组真实数据管理能力。
- 与 `mtab` 的差距开始从“主功能有没有”收缩到“图标样式、字段顺序、交互细节是否完全一致”。

### 风险与后续

- 当前数据快照仍是本地实现，和 `mtab` 的历史恢复展示样式还有差距。
- 左侧菜单图标与部分分区内部排版仍需继续压到 1:1。

## v2.4.28

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续补齐 `mtab` 常规设置与数据变动记录里的实际功能，不只对齐名称和布局。

### 本次修改

- 搜索历史开关接回本地设置，关闭后会清空本地搜索历史缓存。
- 数据变动记录开始记录首页本地快照，配置、标签、Dock、当前页面切换都会生成可恢复的历史节点。
- 设置中心“数据变动记录”分区补回了搜索历史开关和历史快照恢复入口。

### 迁移结果

- 设置中心里“常规设置 / 数据变动记录”不再只是占位结构，已经开始承载与 `mtab` 对标的真实本地功能。
- 现在首页已经具备最基本的“导出备份 / 重置标签 / 历史恢复 / 搜索历史开关”四条数据管理链路。

### 风险与后续

- 历史快照当前仍是本地实现，展示与恢复逻辑还没有完全压到 `mtab` 的交互细节。
- 常规设置里仍有 `mtab` 的部分项未接回，例如更完整的翻译/搜索偏好链路。

## v2.4.27

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：基于本地 `mtab` / `latest/mtab` 真实实现，回收编辑标签弹层与设置中心的结构偏差。

### 本次修改

- 标签编辑/新增弹层改回更接近 `mtab custom` 的字段顺序和文案：
  - 网络地址
  - 获取图标
  - 链接名称
  - 网址简介
  - 文字图标
  - 图片图标
  - 选择图标
  - 背景颜色
  - 内嵌窗口
- 设置中心左侧菜单改成 `常规设置 / 主题标签 / 壁纸设置 / 时间日期 / 数据变动记录 / 关于我们`。
- 常规设置访客页补回登录提示、导出书签备份、重置标签。
- 设置中心主路径补回壁纸库入口、页面管理入口和导出/重置的实际动作链。

### 迁移结果

- 编辑标签和设置中心不再只是“功能接近”，而是开始按本地 `mtab` 基线逐项回收结构和文案差异。
- 当前首页在“拖拽 / 编辑标签 / 设置中心”三条主链路上，已经明显比前一轮更接近目标项目。

### 风险与后续

- 设置中心的视觉细节、图标样式和分区内部字段顺序还没有完全压到 1:1。
- 编辑标签弹层虽然字段结构已经对齐不少，但图标选择区和操作密度还要继续微调。

## v2.4.26

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续补齐标签编辑/新增流程与设置中心字段，缩小与原项目的功能差距。

### 本次修改

- 新增/编辑标签弹层补入了实时预览卡、图标地址输入和“使用站点图标”快捷入口，不再只有极简名称与地址表单。
- 设置中心继续补齐原项目已有但新版缺失的字段：
  - 卡片透明度
  - 列间距
  - 最大列数
  - 标签文字颜色
  - 时间文字颜色
  - 图标底板显示
  - 标签名称显示
  - 底部优先布局
  - 时间组件显示与 24 小时制 / 秒钟 / 月日 / 星期 / 农历 / 干支

### 迁移结果

- 标签编辑流开始具备更接近原项目的“预览 + 图标来源控制”能力，不再只是基础数据录入。
- 设置中心已经从壳层迁移进一步走到字段层，时间和图标外观相关配置明显更完整。

### 风险与后续

- 标签弹层仍然没有完全达到原项目的全部模式与入口细节，后续还要继续对齐。
- 设置中心的菜单结构虽然接近，但字段顺序、分组方式和部分文案还要继续压到 1:1。

## v2.4.25

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：先把首页拖拽从原生硬切换改成更接近 iTab / 原版的平滑换位流程。

### 本次修改

- 首页主网格、Dock 和文件夹内列表都接入了拖拽预览顺序，拖入目标位时会先发生实时换位，再在落下时持久化。
- 新增了基于 FLIP 的位移动画，排序变化不再是瞬间跳位，而是连续过渡。
- 保留现有页面、文件夹、Dock 的业务规则不变，先只替换拖拽时的手感层和排序提交流程。

### 迁移结果

- 主网格内部排序、Dock 内部排序、文件夹内部排序已经从“落下后硬切换”升级到“拖拽时预览换位”。
- 当前实现虽然还没完全达到原项目全部细节，但已经明显缩小了与 iTab / 原版在拖拽连续性上的差距。

### 风险与后续

- 当前跨容器拖拽规则还沿用旧链路，下一轮还要继续压“主网格 -> Dock / 文件夹”的丝滑程度。
- 编辑标签、新增标签和设置中心内容仍然没有完成 1:1，对照复刻要继续推进。

## v2.4.24

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：修复标准模式右上角工具栏被收残的问题，恢复兼容模式方向的按钮组结构。

### 本次修改

- `Toolbar` 恢复标准模式下的账户入口和设置入口，不再只剩一个模式切换按钮。
- 移除了标准态工具栏的误伤隐藏规则，避免账户按钮和设置按钮被 CSS 直接压掉。
- 简洁模式仍继续保留首尾两个入口，中间按钮按规则隐藏。

### 迁移结果

- 标准模式右上角重新具备完整可见的工具栏，不再出现“图标整组丢失”的问题。
- 简洁模式和标准模式的差异重新回到“同一组按钮的精简显隐”，而不是两套完全不同的结构。

### 风险与后续

- 当前标准模式工具栏已经恢复主要入口，但还需要继续对照兼容模式压缩图标顺序、间距和具体图形选择。
- 后续仍需继续补设置中心字段、页面排序/默认页规则和右键菜单。

## v2.4.23

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：修复简洁模式顶部入口与搜索框的明显回归，重新对齐兼容模式的按钮结构。

### 本次修改

- 删除了误伤简洁模式按钮的样式规则，模式切换按钮恢复为标准态与编辑态都可见，游客态首图标也恢复显示。
- 简洁模式搜索框左侧不再使用文字 `select`，改回图标式搜索引擎入口，并通过搜索面板完成引擎切换。
- 搜索框右侧按钮资源从误用的热搜图改回真正的搜索图标，标准模式与简洁模式统一修正。

### 迁移结果

- 简洁模式顶部控制区重新回到“首尾按钮稳定可见”的行为，不再出现模式切换入口被隐藏的回归。
- 搜索框的左右两端重新符合兼容模式语义：左侧是搜索引擎图标入口，右侧是纯搜索动作按钮。

### 风险与后续

- 简洁模式搜索面板的展开细节和动画还没有完全压到兼容模式 1:1。
- 后续仍需继续补设置中心字段、页面排序/默认页规则和右键菜单。

## v2.4.22

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：补齐设置中心里的主题切换能力，并让它作用到新版首页与控制中心壳层。

### 本次修改

- 设置中心新增主题模式开关，支持 `跟随系统 / 浅色 / 深色` 三种模式。
- 主题模式配置接入 `HomeTheme`，并通过首页根节点类名作用到新版页面外壳。
- 新版控制中心、控制菜单、白底卡片和页脚按钮在深色模式下拥有独立的深色样式，不再只依赖默认浅色方案。

### 迁移结果

- 设置中心已经开始承载真正的主题切换能力，而不只是静态外观字段。
- 新版首页主路径首次具备可配置的浅色/深色壳层表现，为后续继续补更多主题相关字段打下基础。

### 风险与后续

- 当前主题切换只作用于新版壳层，不代表兼容模式主题体系已经 1:1 对齐。
- 后续仍需继续补设置中心字段、页面排序/默认页规则和右键菜单。

## v2.4.21

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续推进多页面和设置中心主路径，补齐页面删除回退、页面恢复策略设置项和退出编辑入口。

### 本次修改

- 页面删除后的当前页回退逻辑已明确回到首页页面，而不是只写空字符串。
- 设置中心继续补齐和多页面直接相关的主题/行为字段：
  - 恢复上次页面
  - 页面栏位置
  - 控制中心位置
  - 搜索框显示
  - 搜索推荐
  - 图标搜索结果
- 工具栏补回了“仅编辑态显示”的退出编辑按钮，继续坚持“长按进入编辑、按钮退出编辑”的路径。
- 清理首页主文件中的无用 `Link` 导入，继续压缩入口层残留。

### 迁移结果

- 多页面工作流开始具备更完整的“创建 -> 切页 -> 删除 -> 回首页”路径。
- 设置中心已经开始承载和页面切换策略、页面栏位置相关的实际配置项，而不再只是外壳。

### 风险与后续

- 页面管理的底层数据语义仍然是 `pageGroup` 过渡层，真正的页面语义层还未完全抽出。
- 设置中心虽然继续扩了字段，但与兼容模式的完整字段集、完整布局和完整交互仍有差距。
- 页面排序、默认页规则和右键菜单仍然是下一轮重点。

## v2.4.20

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续补齐多页面闭环，把“页面删除回退”和“页面管理文案”拉到更一致的状态。

### 本次修改

- 删除当前页面时，当前活动页面会明确回退到首页页面 id，而不再只写空字符串。
- 页面创建、更新、删除相关的成功提示文案开始从“分组”语义切到“页面”语义。
- 页面管理弹层补齐了更统一的中文可见文案，并继续保留“打开 / 编辑 / 删除 / 默认页 / 新建页面”这套页面操作语义。

### 迁移结果

- 多页面工作流开始具备“创建后切页、删除后回首页”的连续性。
- 页面相关提示与操作开始从旧分组语义抽离，用户理解成本降低。

### 风险与后续

- 页面管理虽然已具备切页和删除回退，但页面排序、默认页规则和页面切换持久化细节仍需继续收口。
- 设置中心字段 1:1 仍然是下一轮的主任务，尤其是搜索、时间和更多外观项。

## v2.4.19

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续把页面管理从“可见入口”推进到“可执行工作流”，至少补齐创建后切页闭环。

### 本次修改

- `handleSaveGroup` 现在在创建页面后会返回新页面 id，并立即切换当前活动页面。
- 页面管理组件已能接收保存返回值，创建页面后会自动落到新页面，减少创建后还需手动切换的断点。
- 页面管理组件继续保留当前页高亮与“打开”能力，页面切换控制链路更完整。

### 迁移结果

- 页面创建不再停留在“数据已经存在但用户还停在旧页面”的半完成状态。
- 多页面工作流开始具备“创建 -> 切换 -> 查看”的连续性。

### 风险与后续

- 页面删除后的当前页回退规则仍需继续核对与收口。
- 页面管理组件的中文文案和设置中心字段仍然存在旧版语义残留，后续要继续清理。
- 设置中心 1:1 仍是下一阶段主任务。

## v2.4.18

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：让页面管理不只是命名变化，而是开始具备真实的页面控制能力。

### 本次修改

- 页面管理弹层新增“当前页高亮”和“打开”操作，不再只是编辑名称和删除页面。
- 首页固定页现在会以页面管理列表中的固定项出现，并且在当前页命中时参与高亮。
- 页面管理弹层已接入首页当前页状态，用户可以从页面管理中直接切换当前页面。

### 迁移结果

- 页面管理开始从“页面元数据编辑器”转向“页面工作台”。
- 后续继续补页面创建后自动切页、页面删除回退和页面排序时，已有清晰的弹层行为基础。

### 风险与后续

- 页面管理虽然能切页，但创建页面后尚未自动跳转到新页面。
- 页面管理当前仍沿用旧 `pageGroup` 数据结构，真正的页面语义层还需要继续推进。
- 设置中心字段 1:1 和页面切换持久化细节仍需继续收口。

## v2.4.17

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续把多页面能力从“过渡语义”拉向“真实页面语义”，同时保持主路径入口继续收口。

### 本次修改

- 新增 `src/features/home/home-page-manager.tsx`，把原本混在动作弹层里的页面/分组管理逻辑拆出为独立页面管理组件，开始承接多页面语义。
- `home-page.tsx` 已切换为使用新的页面管理组件，不再直接依赖旧的 `PageGroupManagerDialog` 作为主路径入口。
- 首页侧栏仍保留常驻页面入口，页面管理至少在结构上不再只是隐藏在旧分组逻辑中的能力。
- 修复这轮接入过程中产生的构建断点，保证新组件接入后 `typecheck` 与 `build` 继续通过。

### 迁移结果

- 页面管理开始从“旧分组管理组件换个标题继续用”转向“独立组件承接页面语义”的方向。
- 后续继续补页面创建 / 删除 / 切换 / 默认页规则时，有了更稳的组件落点。

### 风险与后续

- 虽然组件入口已拆出，但当前内部仍在复用 `pageGroups` 数据，底层数据语义还没有完全从 `group` 切换为 `page`。
- 页面管理组件的用户可见文案和字段还需要继续清理，避免旧“分组”语义残留。
- 设置中心字段 1:1、页面切换行为和右键菜单仍然是下一步重点。

## v2.4.16

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：校准后续迭代方向，避免在入口、语义和 1:1 复刻目标之间继续漂移。

### 本次修改

- 重写 `plan.md`，把当前未完成工作按“视觉 1:1 / 编辑与拖拽 / 多页面 / 设置中心 / 搜索 / 右键菜单 / 窗口容器 / 账户 / 工程”九个大块重新梳理。
- 把最近几轮新增的要求显式纳入计划，包括：
  - 长按进入编辑作为主路径
  - 多页面语义与入口常驻化
  - 设置中心 1:1
  - 兼容入口彻底退出新版主路径
  - 兼容模式作为唯一校准基线
- 重新定义后续执行顺序，明确先做入口和结构，再补工作流，再压视觉细节，最后回填扩展能力。

### 迁移结果

- 后续方向从“零散收 bug”重新回到“按主链路逐段收口”的节奏。
- 当前计划已经能覆盖后续所有明确未完成项，不再依赖临时记忆推进。

### 风险与后续

- 计划已校准，但页面管理语义、设置中心字段、右键菜单和完整拖拽链路仍然需要继续按计划逐项落代码。
- 后续如果再新增明确要求，必须优先更新 `plan.md` 再继续实现，避免方向再次漂移。

## v2.4.15

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续清理首页主路径里的旧兼容残留，并把多页面入口从隐藏能力变成可见能力。

### 本次修改

- 工具栏中的兼容入口逻辑继续收口，主界面不再保留它作为可见交互路径。
- 显式编辑按钮改为仅在编辑态可见，标准态继续坚持“长按进入编辑”的主路径。
- 侧栏新增了常驻“页面”入口，不再要求先进入编辑模式才能发现多页面管理能力。
- 设置中心中的页面数量修正为至少包含首页，避免出现“页面管理里显示 0 个页面”的错误感知。

### 迁移结果

- 首页顶部的主路径进一步收敛，不再混杂兼容入口与显式编辑入口。
- 多页面能力开始从“存在于数据结构里”转向“用户能直接发现并进入”的状态。
- 后续继续补页面创建、切换和排序行为时，入口层已经基本稳定。

### 风险与后续

- 页面管理弹层目前仍然沿用“分组管理”实现，文案、字段和行为还没有完全升级为“页面管理”语义。
- 设置中心虽然已能挂出页面入口，但完整字段和完整功能与旧版仍有明显差距。
- 仍需继续做页面切换行为、设置按钮触发链路和旧版控制中心细节对齐。

## v2.4.14

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续把设置中心和多页面能力拉进新版主路径，同时保持首页交互链路稳定。

### 本次修改

- 设置中心进一步改造成旧版控制中心方向：底角弹出、左菜单右内容、底部操作栏，不再是居中模态框。
- 设置中心里新增“页面管理”入口，把多页面管理能力接回新版主路径，不再只能从旧编辑流里摸到。
- 设置按钮补了一层 `onMouseDown` 触发，降低某些点击路径下只聚焦不执行的问题。
- 兼容入口继续通过源代码和样式双层收口，不再作为新版主界面的可见路径保留。
- 简洁模式、长按编辑和类型检查链路保持可用，并继续通过 `build` / `typecheck` 验证。

### 迁移结果

- 新版设置中心已经不再是单纯“新设计弹窗”，开始往旧版控制台结构靠拢。
- 多页面管理至少已经在新版设置中心中具备显性入口，为继续补齐页面创建、切换和排序提供了稳定入口。
- 首页主路径继续收敛到“新版入口承载旧版行为”的方向，兼容入口的依赖被继续削弱。

### 风险与后续

- 设置中心当前对齐的是壳层与入口，不是完整字段和全部功能，距离旧版 1:1 还有差距。
- 页面管理虽然已可达，但侧栏新增页面入口和完整页面工作流还要继续收口。
- 旧版右键菜单、设置菜单完整项、以及页面切换的细节动画仍待继续对齐。

## v2.4.13

- 日期：2026-04-06
- 迭代类型：小版本
- 目标：继续把新版首页入口和设置层拉回旧版行为，先处理长按编辑、简洁模式结构和控制中心弹出壳层。

### 本次修改

- 简洁模式下的搜索框已切到“左侧搜索引擎下拉 + 右侧搜索按钮”的旧版结构。
- 首页卡片支持鼠标左键长按进入全局编辑模式，并且在简洁模式下长按会自动切回标准模式，避免没有退出编辑路径。
- 兼容模式入口继续从新版主页主界面移除，侧栏和工具栏不再显式暴露。
- 设置中心从居中模态框改成了旧版控制中心方向的底角弹出结构：左菜单、右内容、底部操作栏，开始向旧版 `control-center / control-model` 结构对齐。
- `typecheck` 链路切到独立的 `tsconfig.typecheck.json`，不再依赖 `.next/types` 的瞬时状态。

### 迁移结果

- 首页的关键交互入口已经不再依赖顶部显式编辑按钮，进入编辑的路径更接近旧版。
- 简洁模式的视觉骨架继续往旧版收敛，兼容入口已经不再干扰新版主路径。
- 设置中心的打开位置和整体层级关系开始接近旧版，为后续继续做 1:1 字段与功能迁移打下基础。

### 风险与后续

- 设置中心当前只是壳层和导航结构贴近旧版，字段范围和全部能力还没有做到 1:1。
- 多页面结构虽然已有 `pageGroup` 数据基础和管理入口，但还需要继续核对旧版的创建入口、默认排序和切换行为。
- 仍需继续对齐旧版右键菜单、文件夹展开态和完整编辑工作流。

## v2.4.12

- 日期：2026-04-05
- 迭代类型：小版本
- 目标：先把首页交互入口继续拉回旧版，优先处理兼容入口移除、长按进入编辑、简洁模式工具栏和简洁模式搜索框结构。

### 本次修改

- 删除首页显性兼容入口的主界面露出：侧栏兼容按钮被移除，用户面板里的空兼容链接也隐藏，简洁模式工具栏只保留首尾两个图标。
- 新增 `use-press-and-hold.ts` 的长按手势接入，首页主网格中的普通卡、动作卡、文件夹卡都开始支持左键长按进入全局编辑模式。
- 进入编辑模式时若当前处于简洁模式，会自动退回标准模式，避免简洁态缺少退出编辑路径。
- 简洁模式搜索框开始改回左侧搜索引擎下拉选择 + 右侧搜索按钮的结构，不再只保留循环切换图标。
- 把用户提供的新壁纸库直接复制进项目静态目录，`country` 已覆盖为默认背景，同时继续复用项目内静态资源路径，不引入外部目录依赖。

### 迁移结果

- 首页编辑入口开始从“顶部显式按钮”转向“卡片长按进入”，交互方向更接近旧版。
- 简洁模式的工具栏结构被明显收紧，兼容入口不再继续污染新版主页主界面。
- 壁纸库已经切到项目内资产，后续打包、部署和预览都不再依赖用户本地目录。

### 风险与后续

- 简洁模式搜索框虽然已切入下拉结构，但还需要继续核对旧版的精确宽度、字体、阴影和展开面板层级。
- 设置中心、多页面入口、完整拖拽链路和旧版 1:1 右键菜单仍未完成，需要继续做下一轮收口。

## v2.4.11

- 日期：2026-04-05
- 迭代类型：小版本
- 目标：先把首页卡片体系从“散落条件判断”收敛成可演进模型，再继续做 1:1 UI 校准。

### 本次修改

- 新增 `docs/home-card-architecture.md`，系统梳理旧版首页的卡片分类、展示态/编辑态/创建态流程，以及新版应采用的卡片领域模型。
- 新增 `src/features/home/home-card-model.ts`，把普通链接卡、应用卡、动作卡、文件夹卡、分组卡的判定和基础能力抽成独立模型层。
- 首页开始接入卡片模型工具，收敛文件夹、动作卡、可编辑能力、可见卡片集合和排序相关判断。
- 按旧版 CSS 的盒模型关系回调新版卡片骨架：卡片容器撑满栅格轨道，但内部图片不再直接拉满卡面高度，减轻标签卡整体偏高和行距失真问题。

### 迁移结果

- 首页卡片不再只是 UI 组件集合，已经开始具备“类型 + 能力 + 容器关系”的领域层基础。
- 后续继续补文件夹、窗口容器、插件卡片和更多动作卡时，不需要再把判断散落进首页主文件。
- 当前 1:1 校准有了更稳定的参照系，后续修高度、宽卡、行距和编辑态浮层会更可控。

### 风险与后续

- 当前卡片模型已经落地为工具层，但渲染层还没有完全拆成 `BaseCardFrame / ActionCard / FolderCard / DockCard` 这种更清晰的组件结构。
- 这次先回收了标签卡高度关系，仍需要继续对照旧版截图核对宽卡和文件夹卡的精确占位。

## v2.4.10

- 日期：2026-04-05
- 迭代类型：小版本
- 目标：补首页文件夹交互缺口，并把本地开发验证链路稳定下来。

### 本次修改

- 首页图标现在支持直接拖到文件夹卡片内，访客本地存储与登录态服务端持久化都会同步更新。
- 文件夹内标签新增“移出文件夹”能力，移动到分类时会真实脱离文件夹并落到目标分组根层，而不是只改 `pageGroup`。
- 移动整个文件夹到其他分组时，会同步修正子项的分组归属，避免父子数据漂移。
- `typecheck` 脚本改为先执行 `next typegen` 再跑 `tsc`，避免首次校验因 `.next/types` 尚未生成而失败。
- `next.config.mjs` 补充 `allowedDevOrigins`，对齐 `localhost` / `127.0.0.1` 双地址的本地开发访问。

### 迁移结果

- 新版首页的文件夹不再只是只读预览容器，已经开始具备旧版“收纳和释放标签”的基础维护能力。
- 本地开发的验证链路变得稳定，`build` 与 `typecheck` 都可以直接作为迭代收口步骤使用。

### 风险与后续

- 当前仍未补齐“文件夹内标签直接拖回桌面根层”的拖放路径，暂时通过右键菜单完成移出。
- Dock 拖到主网格后的落点行为仍然偏保守，后续还需要继续对齐旧版的命中区和反馈细节。

## v2.4.9

- 日期：2026-04-05
- 迭代类型：小版本
- 目标：先把新版首页最明显的布局偏差拉回旧版的固定 icon 栅格节奏。

### 本次修改

- 修正首页尺寸映射逻辑，`1x2` 从错误的纵向大卡恢复为旧版的横向宽卡。
- 新版首页主网格改为固定 icon 宽度、固定列距、按视口只计算列数，不再用会把图标拉大的流式列布局。
- 移除主网格中的重复容器，避免布局叠层导致的尺寸异常。
- 保留当前已迁移的编辑模式、Dock 和动作卡片逻辑，同时让它们运行在新的固定栅格上。

### 迁移结果

- 新版首页图标尺寸已经明显收敛到旧版的视觉比例，不再出现整屏巨大图标卡片。
- 主页结构开始回到旧版“固定节奏”的布局逻辑，为后续继续做 1:1 复刻打下基础。

### 风险与后续

- 当前宽卡、文件夹卡和标签文案仍有局部重叠，说明还有细节样式没完全对齐。
- 下一步需要继续压布局细节，并把拖拽命中区和桌面右键菜单一起拉回旧版行为。

## v2.4.8

- 日期：2026-04-05
- 迭代类型：小版本
- 目标：继续补齐旧版首页对底部 Dock 的维护能力。

### 本次修改

- 新版首页增加 `tabbar` 运行态和持久化逻辑，访客走本地存储，登录用户走 `/tabbar/update`。
- 底部 Dock 改为基于当前运行态渲染，不再只展示初始服务端数据。
- 编辑模式下支持将首页标签加入 Dock。
- 编辑模式下支持从 Dock 中移除标签并在 Dock 内拖拽重排。
- 补充拖拽结束后的状态清理，避免拖放中断后页面残留高亮状态。

### 迁移结果

- 新版首页已经开始具备旧版底部 Dock 的基本维护能力。
- 首页主网格与 Dock 两套快捷入口现在都能在新版内直接维护。

### 风险与后续

- 还没有补 Dock 与主网格之间更完整的拖放联动。
- 旧版更细的拖拽反馈和桌面右键菜单仍待迁移。

## v2.4.7

- 日期：2026-04-05
- 迭代类型：小版本
- 目标：继续把旧版首页的桌面编辑能力迁移到新版首页。

### 本次修改

- 扩展 `src/features/home/home-actions.tsx`，统一承载标签编辑器和分组管理器。
- 首页新增编辑模式，进入后可直接编辑、删除当前可见标签。
- 当前分组内的网格项目支持原生拖拽排序，排序后会同步写回服务端或本地存储。
- 侧边栏新增分组管理入口，并支持创建、编辑、删除分组。
- 标签编辑器新增分组归属选择，允许把标签在首页与不同分组之间迁移。

### 迁移结果

- 新版首页已经开始具备旧版“桌面编辑”核心路径，不再只是查看与少量快捷操作。
- 首页当前可见分组中的标签可以直接在新版页面里完成维护。

### 风险与后续

- 当前拖拽排序先覆盖当前分组内主网格，底部 Dock 和更复杂的嵌套场景仍未接管。
- 旧版更完整的桌面右键菜单、分组内细节行为还需要继续补齐。

## v2.4.6

- 日期：2026-04-04
- 迭代类型：小版本
- 目标：恢复旧首页网格中的特殊动作入口，让新版首页继续逼近旧版交互路径。

### 本次修改

- 新增 `src/features/home/home-actions.tsx`，实现新版首页的动作卡片弹层。
- 网格重新展示旧版的 `添加标签`、`壁纸`、`设置` 三个动作入口，不再直接过滤掉 `tab://` 特殊链接。
- `添加标签` 现在可以在新版首页内直接新增导航项，并按登录状态写回服务端或本地浏览器。
- `壁纸` 现在可以在新版首页内切换默认背景、静态背景库图片和自定义链接背景。
- 未登录用户的首页链接数据开始通过本地 `link` 存储保持，和旧版行为更接近。

### 迁移结果

- 新版首页已经不只是“展示旧数据”，而是能直接执行旧首页里的核心入口动作。
- 旧版首页网格里的几个高频动作现在有了对应的 TypeScript 实现。

### 风险与后续

- 当前 `添加标签` 仅覆盖了新版内最基础的新增导航能力，还没有补拖拽、删除、编辑。
- 背景管理目前主要接管用户侧切换逻辑，旧版更完整的壁纸管理能力仍需继续拆。

## v2.4.5

- 日期：2026-04-04
- 迭代类型：小版本
- 目标：继续让新版首页接管旧版首页可见配置能力，减少对 `tab://setting` 的依赖。

### 本次修改

- 新增 `src/features/home/home-settings.tsx`，构建新版首页设置中心。
- 支持布局、搜索、时间、外观四组核心配置项的可视化编辑。
- 首页改为以 `currentConfig` 作为客户端运行态，配置修改可以即时反映到页面。
- 未登录用户通过本地 `config` 存储保持配置，已登录用户通过 `/config/update` 写回服务端。
- 工具栏新增设置入口，首页不再只有兼容入口才能修改主要展示配置。

### 迁移结果

- 新版首页已经具备旧版首页最核心的展示配置能力。
- 访客和登录用户都可以在新版页面内直接调整首页主要行为，不必退回旧版设置面板。

### 风险与后续

- 目前设置中心仍未覆盖旧后台里的所有站点级配置项。
- 背景库、更多个性化外观设置、第三方登录细项和桌面编辑能力还需要继续迁移。

## v2.4.4

- 日期：2026-04-04
- 迭代类型：小版本
- 目标：开始把旧首页从 `public/dist` 编译产物迁移到可维护的 Next.js + TypeScript 页面与组件。

### 本次修改

- 新增 `src/server/home` 首页数据服务，语义化读取站点设置、默认首页配置、用户首页配置与快捷栏数据。
- 新增 `src/features/home/home-page.tsx` 与 CSS Module，使用 TypeScript 组件重建首页首屏。
- 新增 `app/page.tsx`，让根路径直接由 Next.js 页面接管，不再返回旧 `dist/index.html`。
- 保留 `app/[...path]/route.ts` 与 legacy handler，其余接口和兼容路由暂不动，降低迁移风险。
- 新增 `plan.md` 与本维护文档，开始按迭代记录推进迁移。
- 新增首页客户端请求层与 toast 机制，后续迁移交互时不再直接依赖旧前端状态机。
- 新增首页登录/注册/重置密码弹层、用户菜单与退出登录流程，直接复用现有 legacy API。
- 新增首页全局通知展示能力，并把通知内容改为服务端读配置后直接注入首页数据。
- 移除 `next.config.mjs` 中与当前启动方式冲突的 `standalone` 输出配置，消除生产启动警告。

### 迁移结果

- 首页首屏已经具备独立的 TypeScript 源码入口。
- 页面保留原有视觉方向：背景图、时间区、搜索栏、导航网格、底部 Dock 与备案信息。
- 旧版完整能力仍可通过兼容入口访问，避免首批迁移阻断现有使用路径。
- 新版首页已经具备基本账户入口，可以直接进行登录、注册、密码重置与退出。
- 生产构建与常规 `next start` 启动方式已经重新对齐。

### 风险与后续

- 目前尚未迁移登录弹层、设置中心、拖拽排序、插件容器等高交互能力。
- 仍有大量首页业务逻辑留在 `src/server/legacy/handler.ts`，后续需要继续拆分。
